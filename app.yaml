runtime: nodejs14
env_variables:
  NODE_ENV: production
handlers:
  - url: /
    static_files: build/index.html
    upload: build/index.html
  - url: /(.*)
    static_files: build/\1
    upload: build/(.*)
